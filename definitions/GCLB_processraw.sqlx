
config {
 type: "incremental",
  bigquery: {
    partitionBy: "TIMESTAMP_TRUNC(receiveTimestamp, HOUR)"
  }
}

SELECT * FROM ${ref("gclbimport")}

${when(incremental(), `WHERE receiveTimestamp > (SELECT MAX(receiveTimestamp) FROM ${self()})`) }